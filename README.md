# Usage

- Clone the repo.
- This project is managed by [uv](https://docs.astral.sh/uv/). To install dependencies,
  ```shell
  $ uv sync --no-dev
  ```
- To run `ces` module,
  ```shell
  $ ./main.py
  ```
